class Student {

  studentId: number;
  firstName: string;
  lastName: string;
  date: Date;


  constructor(studentId: number, firstName: string, lastName: string, date: Date) {
    this.studentId = studentId;
    this.firstName = firstName;
    this.lastName = lastName;
    this.date = date;
  }
}
